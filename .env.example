# Discord Bot Token (Discord Developer Portal > Bot > Token)
DISCORD_TOKEN=YOUR_DISCORD_BOT_TOKEN

# Application (Client) ID (Discord Developer Portal > General Information)
CLIENT_ID=YOUR_DISCORD_APPLICATION_CLIENT_ID

# Development Guild ID (recommended for fast command updates)
# Set this to deploy slash commands to a specific guild instantly.
# Leave empty to deploy globally (can take up to about 1 hour to reflect).
GUILD_ID=YOUR_DISCORD_GUILD_ID

# TTS Server Host (e.g. voicevox, coeiroink-voice, etc.)
# For local bot run (pnpm dev): 127.0.0.1
# For docker bot run: voicevox
TTS_HOST="127.0.0.1"
TTS_PORT="50021"
